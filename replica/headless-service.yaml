apiVersion: v1
kind: Service
metadata:
  name: postgres-replica-headless
  namespace: postgres-ha
  labels:
    app: postgres-replica
spec:
  clusterIP: None
  selector:
    app: postgres-replica
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
