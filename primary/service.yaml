apiVersion: v1
kind: Service
metadata:
  name: postgres-primary
  namespace: postgres-ha
  labels:
    app: postgres-primary
spec:
  type: ClusterIP
  selector:
    app: postgres-primary
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
