apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-primary-config
  namespace: postgres-ha
  labels:
    app: postgres-primary
data:
  postgresql.conf: |
    listen_addresses = '*'
    port = 5432
    max_connections = 200
    wal_level = replica
    max_wal_senders = 10
    max_replication_slots = 10
    wal_keep_size = 512MB
    hot_standby = on
    synchronous_commit = on
    log_destination = 'stderr'
    logging_collector = off
    log_min_duration_statement = 0
    log_connections = on
    log_disconnections = on
    log_line_prefix = '%t [%p]: [%l-1] '

  pg_hba.conf: |
    local   all             all                                     trust
    host    all             all             127.0.0.1/32            scram-sha-256
    host    all             all             ::1/128                 scram-sha-256
    host    all             all             0.0.0.0/0               scram-sha-256
    host    replication     pg_replica      0.0.0.0/0               scram-sha-256

  init-replication-user.sql: |
    DO
    $$
    BEGIN
      IF NOT EXISTS (SELECT FROM pg_catalog.pg_roles WHERE rolname = 'pg_replica') THEN
        CREATE ROLE pg_replica WITH REPLICATION LOGIN PASSWORD 'Replica@2026';
      END IF;
    END
    $$;

    SELECT pg_create_physical_replication_slot('replica1_slot')
    WHERE NOT EXISTS (
      SELECT 1 FROM pg_replication_slots WHERE slot_name = 'replica1_slot'
    );